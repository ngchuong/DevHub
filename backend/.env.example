# Database Connection URL
# For running backend locally (outside Docker) connecting to database in Docker:
DATABASE_URL="postgresql://admin:secret@localhost:5432/devhub_db"

# For running backend inside Docker (this is already set in docker-compose.yml):
# DATABASE_URL="postgresql://admin:secret@postgres:5432/devhub_db"
